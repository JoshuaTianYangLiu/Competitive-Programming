// dp?
// Given a sorted segment, we know the distance of any two values will be within that length
// As n<=200 we got a ton of wiggle room

#include <bits/stdc++.h>

using namespace std;

int main() {
    int t;
    scanf("%d",&t);
    while(t--){
        int n;
        scanf("%d",&n);
        for(int i=1; i<=n; i++){
            int k;
            scanf("%d",&k);

        }
    }
    return 0;
}